import Editor from '../../components/Editor'

const New = () => {
  return (
    // TODO: allow user to create own color scheme
    // TODO: allow user to upload own template to server
    <Editor />
  )
}

export default New

